This DLL is used to get a count of the number of physical processors and how
many logical processors there are per physical processors. This is necessary
because otherwise we can not distinguish between a Pentium 4 w/ HyperThreading
and a normal dual CPU box.